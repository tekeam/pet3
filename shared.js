const D=window.NINJA_DATA;const qs=n=>new URLSearchParams(location.search).get(n);const brandOf=s=>D.brands.find(b=>b.slug===s);const productOf=s=>D.products.find(p=>p.slug===s);const esc=s=>String(s??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));function productCard(p){const b=brandOf(p.brand);return `<a class="product-card" href="product.html?slug=${encodeURIComponent(p.slug)}"><span class="tag">${esc(p.need.split('،')[0])}</span><div class="product-thumb"><div class="mini-bag" style="background:${p.color}">${esc(b?.name||p.brand)}<br><small>${esc(p.name)}</small></div></div><div class="product-info"><small>${esc(b?.fa||'')}</small><h3>${esc(p.fa)}</h3><p>${esc(p.desc)}</p><div class="card-meta"><span>${p.pet==='cat'?'گربه':'سگ'}</span><span>${esc(p.age)}</span><span>${esc(p.breed)}</span></div></div></a>`}function brandCard(b){return `<a class="brand-card" href="brand.html?slug=${encodeURIComponent(b.slug)}"><strong>${esc(b.name)}</strong><small>${esc(b.fa)}</small></a>`}function fillHeader(active=''){document.querySelectorAll('.desktop-nav a,.mobile-bottom a').forEach(a=>{if(a.dataset.nav===active)a.classList.add('active')});}function setupSearch(){const input=document.querySelector('[data-smart-search]'),box=document.querySelector('[data-search-results]');if(!input||!box)return;const run=()=>{const q=input.value.trim().toLowerCase();const age=(document.querySelector('[data-search-age]')?.value||'all'),pet=(document.querySelector('[data-search-pet]')?.value||'all'),breed=(document.querySelector('[data-search-breed]')?.value||'').trim().toLowerCase();let hits=D.products.filter(p=>{const b=brandOf(p.brand);const hay=[p.fa,p.name,p.desc,p.need,p.breed,p.age,b?.fa,b?.name].join(' ').toLowerCase();return(!q||hay.includes(q))&&(age==='all'||p.age===age)&&(pet==='all'||p.pet===pet)&&(!breed||p.breed.toLowerCase().includes(breed))});box.innerHTML=hits.slice(0,20).map(productCard).join('')||'<div class="empty-state">نتیجه‌ای پیدا نشد.</div>'};input.addEventListener('input',run);document.querySelectorAll('[data-search-age],[data-search-pet],[data-search-breed]').forEach(x=>x.addEventListener('input',run));run()}function makeSlug(s){return String(s).trim().toLowerCase().replace(/[^؀-ۿa-z0-9]+/g,'-').replace(/^-|-$/g,'')}window.Ninja={D,qs,brandOf,productOf,productCard,brandCard,fillHeader,setupSearch,makeSlug};