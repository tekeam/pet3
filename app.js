const {D,productCard,brandCard}=Ninja;const brandGrid=document.querySelector('#brandGrid'),productGrid=document.querySelector('#productGrid'),brandFilter=document.querySelector('#brandFilter');function renderBrands(limit=6){brandGrid.innerHTML=D.brands.slice(0,limit).map(brandCard).join('')}function renderProducts(list=D.products){productGrid.innerHTML=list.map(productCard).join('')}renderBrands();renderProducts(D.products.slice(0,8));D.brands.forEach(b=>brandFilter.insertAdjacentHTML('beforeend',`<option value="${b.slug}">${b.fa}</option>`));document.querySelector('#allBrands').addEventListener('click',()=>location.href='brands.html');document.querySelectorAll('.chips button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.chips button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;renderProducts(f==='all'?D.products.slice(0,12):D.products.filter(p=>f==='medical'?p.need.includes('درمانی'):p.pet===f).slice(0,12))}));document.querySelector('#finderButton').addEventListener('click',()=>{const pet=document.querySelector('#petType').value,brand=brandFilter.value,age=document.querySelector('#ageFilter').value;const params=new URLSearchParams();if(pet!=='all')params.set('pet',pet);if(brand!=='all')params.set('brand',brand);if(age!=='all')params.set('age',age);location.href=`search.html?${params}`});document.querySelector('[data-search-open]').addEventListener('click',()=>location.href='search.html');document.querySelector('#vetSearch').addEventListener('click',()=>{const p=document.querySelector('#province').value,c=document.querySelector('#city').value;location.href=`veterinarians.html?province=${encodeURIComponent(p)}&city=${encodeURIComponent(c)}`});document.querySelector('#province').innerHTML='<option value="all">استان</option>'+Object.keys(D.locations).map(x=>`<option>${x}</option>`).join('');document.querySelector('#province').addEventListener('change',e=>{const list=D.locations[e.target.value]||[];document.querySelector('#city').innerHTML='<option value="all">شهر</option>'+list.map(x=>`<option>${x}</option>`).join('')});document.querySelector('#newsletter').addEventListener('submit',e=>{e.preventDefault();alert('ایمیل شما ثبت شد.');e.target.reset()});if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});